This is the code necessary to reproduce the model used in the prediction application.

Analyses were performed using R 4.3.1. Exact package versions are recorded in
`renv.lock`, with additional environment information in `sessionInfo.txt`.

To restore the R package environment:

install.packages("renv")
renv::restore()
